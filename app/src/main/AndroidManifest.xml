<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.teachable">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:name=".TeachableApp">
        <activity
            android:name="messages.ChatLogActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="messages.NavHomeActivity" />
        </activity>

        <activity android:name="messages.NewMessageActivity">

            <!-- Generate a back button -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="messages.NavHomeActivity" />
        </activity>
        <activity
            android:name="messages.NavHomeActivity"
            android:theme="@style/NavHomeTheme"
            android:hardwareAccelerated="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="registerlogin.LoginActivity" />
        <activity android:name="registerlogin.RegisterActivity" />
    </application>

</manifest>